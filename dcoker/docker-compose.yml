version: '3.8'

services:
  kangni-agents:
    build: .
    container_name: kangni-agents
    volumes:
      - ./src:/app/src
      - ./test_rag.py:/app/test_rag.py
    environment:
      - PYTHONPATH=/app/src